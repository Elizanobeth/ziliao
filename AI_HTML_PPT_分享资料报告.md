# 用 AI 制作 HTML 演示文稿：分享资料报告

## 摘要

HTML 演示文稿（也常被称为“网页 PPT”）是把演示内容制作成可在浏览器中打开和播放的网页。它并不等同于传统的 `.pptx` 文件，也不意味着要让每位同事学习编程。对大多数使用者而言，更实际的工作方式是：**人负责目标、内容和判断，AI 负责把明确的要求转成可预览、可迭代的 HTML 页面。**

本文围绕四个问题说明基本概念、与传统 PPT 的差异、AI 为什么常更适合协作生成 HTML，以及如何用清晰的提示词产出可靠的页面。文末提供可直接复用的提示词与检查清单。

---

## 1. 什么是 HTML

### 1.1 一句话定义

HTML 是 **HyperText Markup Language**（超文本标记语言）的缩写，是描述网页内容结构的标准语言。它用标签标明“这是标题”“这是段落”“这是图片”“这是列表”或“这是表格”，浏览器再把这些内容显示出来。

MDN 对 HTML 的定义是：它用于定义网页内容的含义和结构；网页的表现通常由 CSS 描述，功能与行为通常由 JavaScript 实现。[MDN：HTML](https://developer.mozilla.org/zh-CN/docs/Web/HTML)

可以用一个办公场景的比喻理解：

- HTML 像演示稿的**目录和内容骨架**；
- CSS 像视觉规范，决定字体、颜色、间距、版式；
- JavaScript 像交互控制，决定点击、翻页、动画或数据更新。

```html
<h1>第二季度项目复盘</h1>
<p>核心结论：交付速度提升，但客户留存仍需重点改善。</p>
```

上面的代码表达的并非设计坐标，而是内容的语义关系：第一行是一级标题，第二行是一段正文。浏览器会根据样式将其渲染成视觉页面。

### 1.2 什么是 HTML 演示文稿

HTML 演示文稿是由一个或多个网页页面组成、按照幻灯片节奏播放的内容。常见形式有：

- 单个 `.html` 文件，双击或拖入浏览器即可打开；
- 一个网页链接，适合内部分享或嵌入知识库；
- 基于演示框架制作的网页，例如 Reveal.js。

Reveal.js 是开源的 HTML 演示框架，明确将自己定义为“HTML presentation framework”，并基于开放的 Web 技术构建演示稿。[Reveal.js 官网](https://revealjs.com/)

它可以具有封面、目录、内容页、图表、动画和翻页控制；在投屏时，使用浏览器全屏与左右方向键就能完成常规演示。

### 1.3 不需要把它神化

HTML 不是“更高级的 PPT”，也不是自动解决内容质量的工具。它只是另一种文件和呈现方式。网页做得再炫，如果结论不清楚、数据不可靠、文字太多，仍然是一份不好的演示稿。

---

## 2. HTML 和 PPT 有什么区别

### 2.1 核心差异

| 维度 | HTML 演示文稿 | 传统 PPT |
| --- | --- | --- |
| 文件形态 | `.html`、网页资源或链接 | `.pptx` 文件 |
| 打开方式 | 浏览器 | PowerPoint、WPS 等办公软件 |
| 页面构成 | 结构化文本 + 样式 + 可选脚本 | 幻灯片画布上的文本框、形状、图片等对象 |
| 常见编辑方式 | 改代码，或用自然语言让 AI 修改代码 | 鼠标拖拽、调整对象属性 |
| 交互能力 | 可方便接入网页动画、表单、实时数据和按钮 | 擅长传统切换、动画、备注、批注和 Office 工作流 |
| 发布方式 | 可部署为链接、嵌入网页、全屏播放 | 文件发送、云端协作、会议软件直接共享 |
| 兼容性重点 | 浏览器、屏幕尺寸、网络与资源路径 | Office/WPS 版本、字体、主题与对象兼容 |

### 2.2 两者各自的优势

**HTML 演示文稿更合适的场景**

- 技术分享、产品展示、内部方案讲解；
- 想加入网页交互、可点击流程、动态图表或实时数据；
- 希望把演示内容发布为链接，并持续更新；
- 团队可以接受浏览器作为演示环境；
- 需要高频、批量地调整内容或统一视觉规范。

**传统 PPT 更合适的场景**

- 客户或领导明确要求交付可编辑 `.pptx`；
- 公司已有严格的 PPT 母版、品牌模板和审阅流程；
- 需要复杂的演讲者备注、现场标注、多人在 Office 中协作；
- 需要离线、跨设备、低技术门槛的稳妥交付。

PowerPoint 的演讲者视图可显示当前页、下一页、备注、计时和标注工具，这类成熟的演讲辅助能力是传统 PPT 的强项。[Microsoft：使用演讲者视图](https://support.microsoft.com/en-gb/office/what-is-presenter-view-98f31265-9630-41a7-a3f1-9b4736928ee3)

### 2.3 一个容易误解的点：HTML 不等于“只能在线”

许多 HTML 演示稿可以打包成一个单文件或本地文件夹，离线使用浏览器打开。真正需要提前检查的是：图片、字体、脚本或数据是否依赖外部网络资源。若演示场地网络不稳定，应在演示前离线测试，并尽量将必要资源嵌入或随文件打包。

---

## 3. 为什么 AI 通常更适合协作写 HTML，而不是直接写 PPT

### 3.1 先说结论

准确的表达不是“AI 不能写 PPT”，而是：**当目标是从文字需求快速得到一个可播放、可反复修改的演示原型时，AI 与 HTML 的协作通常更自然。**

AI 也可以生成 PowerPoint 文件。例如，PptxGenJS 能通过 JavaScript 生成 `.pptx` 演示文稿。[PptxGenJS 文档](https://gitbrent.github.io/PptxGenJS/)

区别在于，HTML/CSS/JavaScript 都是文本形式，模型可以直接阅读、生成和修改；而 PPTX 的内部是包含 XML、对象坐标、主题、媒体和兼容性信息的压缩包结构。生成或修改 PPTX 时，程序仍需处理很多“对象级”细节。

### 3.2 AI 与 HTML 配合顺畅的四个原因

#### 原因一：需求、代码与反馈都可表达为文本

使用者可以说：“把第 4 页改成三张数据卡片，主色换成深蓝，结论提前放在标题里。”AI 可以将这句反馈映射到 HTML 结构和 CSS 样式。

相比之下，在 PPT 中，同一句话往往会变成大量具体操作：新建形状、调整坐标、对齐、修改字体、复制格式、处理图层关系等。

#### 原因二：样式可以集中控制

HTML 的 CSS 可以用一套规则统一标题字号、正文间距、配色、页码与卡片样式。一处规则更新，整份演示稿都可同步变化。

这很适合 AI 的“全局修改”能力，例如：

> 将所有页面改为白底、深蓝标题、橙色只用于强调；正文不小于 24px；图表统一使用同一组颜色。

#### 原因三：可以快速预览—反馈—修复

HTML 保存后可直接在浏览器打开。若发现文字溢出、元素遮挡、翻页无效，使用者可以给出具体观察，AI 再针对性修复。

```mermaid
flowchart LR
    A[人：目标、材料与约束] --> B[AI：先生成大纲]
    B --> C[人：确认逻辑与事实]
    C --> D[AI：生成 HTML/CSS/JS]
    D --> E[浏览器预览]
    E --> F[人：指出具体问题]
    F --> D
    E --> G[人工验收并演示]
```

#### 原因四：更容易形成团队模板

一套经过验证的 HTML 模板可以固定导航、封面、图表卡片、页脚、品牌色和字号。之后只需让 AI 按模板替换内容，输出稳定性会比“每次从零设计”高很多。

### 3.3 边界：HTML 不会自动带来更好的设计

以下情况，HTML 可能反而不如 PPT 方便：

- 非技术用户需要频繁手动拖拽微调；
- 同事无法或不愿处理浏览器、文件资源和代码；
- 必须与客户反复交换可编辑 PPT；
- 页面高度依赖复杂图片、特殊字体或本地视频；
- 演示环境受限，只允许 Office 文件。

因此，团队应把 HTML 作为一种**适合 AI 协作的演示路径**，而不是强迫所有场景换掉 PPT。

---

## 4. 如何指导 AI 生成想要的 HTML 格式 PPT

### 4.1 先给 AI 一份“设计任务书”

很多生成失败不是因为 AI 不会写 HTML，而是需求只有“帮我做一份好看的 PPT”。“好看”无法替代必要的业务和视觉约束。

至少说明以下六项：

1. **受众**：管理层、客户、技术同事还是新员工？
2. **目的**：汇报、说服、决策、培训还是宣传？
3. **核心结论**：听众离开时只记得一句什么？
4. **事实材料**：真实数据、时间、案例、图片、品牌要求；哪些不能编造？
5. **内容范围**：总页数、每页重点、必须出现和不能出现的内容。
6. **交付要求**：16:9、单文件或多文件、离线使用、是否需要翻页、是否使用动画。

### 4.2 采用“两阶段生成”而不是一键到底

#### 阶段一：先产出大纲

先让 AI 做逻辑，不要急于要求页面代码。此时重点是检查：

- 页面顺序是否符合听众的理解路径；
- 标题是否是结论，而不仅是栏目名；
- 是否有重复页面或没有依据的结论；
- 信息量是否适合目标页数。

**大纲提示词模板：**

```text
你是一名企业演示文稿策划师。

请为“[主题]”设计一份 [页数] 页演示文稿大纲。
受众：[受众]。
目标：[演示目标]。
听众最后应记住的核心结论：[一句话结论]。
已知事实和数据：[粘贴资料；未提供的数据不得虚构]。

每页请输出：
1. 页面标题（直接表达结论，避免只写栏目名）
2. 三条以内内容要点
3. 合适的视觉形式（数据卡片、柱状图、时间线、流程图、对比表等）

先不要生成 HTML 代码。请指出信息缺口和可能需要确认的事实。
```

#### 阶段二：基于已确认大纲生成 HTML

**生成提示词模板：**

```text
基于已确认的大纲，生成一份完整的单文件 HTML 演示文稿。

技术要求：
- 16:9 比例，适合浏览器全屏投屏；
- 支持键盘左右方向键翻页，并显示页码；
- 所有 CSS 与 JavaScript 放在同一个 HTML 文件中；
- 使用纯 CSS 或 SVG 绘制图表，不依赖外部图片、字体或网络资源；
- 页面在 1366×768 和 1920×1080 屏幕下都应可读；
- 正文字号不小于 24px，每页只表达一个重点；
- 不得虚构数据；缺失数据请标为“待补充”。

视觉要求：
- 风格：[例如，克制的企业汇报风格]；
- 主色：[例如，#123B6D]；强调色：[例如，#F59E0B]；
- 保留足够留白，避免堆叠装饰和过度动画。

请输出完整、可直接保存为 .html 后打开的代码。
```

### 4.3 用“可执行反馈”推动迭代

不要说“更高级一些”“再好看一点”。这类话缺少判断标准。应描述：**哪一页、哪里有问题、改成什么、为什么。**

| 不够具体的说法 | 更可执行的说法 |
| --- | --- |
| “第 3 页不好看。” | “第 3 页删除大段说明，保留三项关键数字，改成横向三列数据卡片。” |
| “图表再改一下。” | “第 5 页改为按季度对比的柱状图，直接标出同比增幅，并把结论写进标题。” |
| “整体专业一点。” | “改为咨询汇报风格：白底、深蓝标题、减少圆角和阴影、增加留白、统一页脚。” |
| “内容太多。” | “每页正文最多三条，每条不超过 20 字；详细解释移到演讲备注或附录。” |

### 4.4 示例：将数据和图片放进 HTML 演示文稿

假设要制作一页“第二季度销售复盘”，已有真实数据：

| 月份 | 销售额（万元） | 目标完成率 |
| --- | ---: | ---: |
| 4 月 | 86 | 92% |
| 5 月 | 103 | 108% |
| 6 月 | 121 | 115% |

同时还想展示一张“客户活动现场图”。正确做法不是只对 AI 说“加一个好看的图表和图片”，而是把数据、图片用途、位置和技术要求说完整。

#### A. 将数据交给 AI

数据量不大时，直接以表格形式粘贴到提示词中即可；数据量大时，建议先整理为 CSV 或 Excel，再把所需字段、口径和结论写清楚。无论哪种方式，都应明确：**只能使用提供的数据，不得自行补造数据。**

```text
请在“第二季度销售复盘”中制作一页数据页，标题为：
“销售额连续三个月增长，6 月目标完成率达到 115%”。

只能使用以下数据，不得虚构或补充其他数据：
| 月份 | 销售额（万元） | 目标完成率 |
| 4 月 | 86 | 92% |
| 5 月 | 103 | 108% |
| 6 月 | 121 | 115% |

页面布局：左侧为销售额柱状图，右侧为三条关键结论。
柱状图横轴为月份，纵轴为销售额（万元），每根柱子标出具体数值。
在右侧明确写出：6 月销售额较 4 月增长 35 万元；仅 4 月未完成目标；增长趋势需在下季度持续验证。
请用内嵌 SVG 或 CSS 绘制图表，不要调用在线图表库。
```

这里“增长 35 万元”来自 `121 - 86`，仍建议由人复核后再写入提示词。若不希望 AI 做任何计算，可以将结论也作为已确认事实直接提供。

#### B. 插入图片：两种常用方式

**方式 1：图片与 HTML 放在同一文件夹（最适合日常制作）**

文件结构示例：

```text
销售复盘演示/
├── 销售复盘.html
└── images/
    └── 客户活动现场.jpg
```

HTML 中用相对路径引用图片：

```html
<img src="images/客户活动现场.jpg" alt="客户活动现场" />
```

交给 AI 时可以这样说：

```text
请在第 6 页右侧插入本地图片 `images/客户活动现场.jpg`，
图片用途是展示客户参与活动的真实场景。
请裁切为 4:3 比例、圆角 16px，保留人物主体；
在图片下方添加说明：“6 月客户交流活动现场”。
不要使用外部图片链接，也不要生成或替换这张图片。
```

注意：移动或发送 HTML 时，必须连同 `images` 文件夹一起发送，否则图片会失效。

**方式 2：把图片嵌入单个 HTML 文件（最适合单文件交付）**

图片可以转换为 Base64 数据并直接写进 HTML。这样只有一个文件，便于发送和离线打开；代价是文件会明显变大，图片替换也较麻烦。可对 AI 提出：

```text
请将我提供的图片嵌入到单个 HTML 文件中，不使用外部 URL。
如需使用图片数据，请采用 Base64 data URI，并保留 alt 文本。
```

嵌入后的形式大致如下（内容已省略）：

```html
<img
  src="data:image/jpeg;base64,/9j/4AAQ..."
  alt="客户活动现场"
/>
```

若同事不熟悉 Base64，无需手动转换：将图片文件和明确的版式要求一并提供给具备读图能力的 AI 工具，或请开发同事/工具完成嵌入即可。重点是先决定交付方式：**要单文件，就嵌入；允许文件夹交付，就用相对路径。**

#### C. 综合提示词：一页含数据和图片的 HTML 页面

```text
请在现有 HTML 演示文稿中新增第 6 页，主题为“销售增长来自重点客户活动”。

已确认数据：
- 4 月销售额：86 万元，目标完成率：92%
- 5 月销售额：103 万元，目标完成率：108%
- 6 月销售额：121 万元，目标完成率：115%

已确认结论：销售额连续三个月增长；6 月比 4 月增加 35 万元。

页面要求：
- 标题直接写出结论；
- 左侧用 SVG 柱状图展示三个月销售额，并显示数据标签；
- 右上角引用本地图片 `images/客户活动现场.jpg`，裁切为 4:3；
- 右下角列出不超过三条关键结论；
- 图片下方标注“6 月客户交流活动现场”；
- 图片缺失时显示文字占位提示，不影响其他内容和翻页；
- 不使用外部网络资源，不虚构数据。
```

#### D. 使用数据和图片时的检查点

- 图表单位、时间范围和数据口径是否已写清；
- 计算结果是否由人复核；
- 图片是否确实存在于指定路径，且随演示稿一同交付；
- 图片是否获得使用许可，是否含有客户、员工或其他敏感信息；
- 图片上的人物、文字和关键场景在投屏时是否清晰可辨。

## 5. 演示前检查清单

### 内容

- [ ] 封面、标题和结尾是否服务于一个明确结论？
- [ ] 每页是否只有一个重点？
- [ ] 所有数据、日期、百分比和引用是否人工核实？
- [ ] 是否删除了未经确认的 AI 编造内容？

### 视觉

- [ ] 字号是否适合远距离投屏阅读？
- [ ] 是否存在文字溢出、元素遮挡或对比度不足？
- [ ] 配色、图标与图表是否遵循团队品牌规范？
- [ ] 图表是否包含单位、时间范围和必要的数据说明？

### 技术与合规

- [ ] 是否能在目标浏览器全屏打开并正常翻页？
- [ ] 是否已在没有网络的情况下测试过？
- [ ] 是否不存在失效的外部图片、字体或脚本链接？
- [ ] 是否已审查敏感信息、第三方素材和版权风险？

---

## 参考资料

1. [MDN Web Docs：HTML（中文）](https://developer.mozilla.org/zh-CN/docs/Web/HTML) —— HTML 的定义、内容结构与 CSS/JavaScript 的分工。
2. [Reveal.js](https://revealjs.com/) —— 开源 HTML 演示框架，展示网页演示稿的典型实现方式。
3. [Microsoft Support：使用演讲者视图](https://support.microsoft.com/en-gb/office/what-is-presenter-view-98f31265-9630-41a7-a3f1-9b4736928ee3) —— PowerPoint 演讲者视图及其备注、下一页、计时等能力。
4. [PptxGenJS Documentation](https://gitbrent.github.io/PptxGenJS/) —— 使用 JavaScript 生成 PowerPoint 的技术路径，说明 AI 并非不能生成 `.pptx`。
